[
  {
    "type": "function",
    "function": {
      "name": "list_directory",
      "description": "แสดงรายการไฟล์และโฟลเดอร์ในเส้นทางที่กำหนด",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "เส้นทางที่ต้องการดู"
          }
        },
        "required": [
          "path"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_files",
      "description": "ค้นหาไฟล์ตามคำค้นหาและรูปแบบไฟล์",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "คำค้นหา"
          },
          "file_pattern": {
            "type": "string",
            "description": "รูปแบบไฟล์ (เช่น *.py)"
          }
        },
        "required": [
          "query"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "analyze_project_structure",
      "description": "วิเคราะห์โครงสร้างโปรเจกต์และให้คำแนะนำ",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "เส้นทางโปรเจกต์"
          }
        },
        "required": [
          "path"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "run_code",
      "description": "รันโค้ด Python และส่งคืนผลลัพธ์",
      "parameters": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "โค้ด Python ที่ต้องการรัน"
          },
          "language": {
            "type": "string",
            "description": "ภาษาโปรแกรม (default: python)"
          }
        },
        "required": [
          "code"
        ]
      }
    }
  }
]