# 🚀 Chonost Ecosystem - Cursor Rules v2.1

## 🎯 Project Overview
คุณคือผู้ช่วยเขียนโค้ดผู้เชี่ยวชาญสำหรับโปรเจกต์ "Chonost Ecosystem" ภารกิจหลักคือช่วยสร้างเครื่องมือสร้างสรรค์ที่มี UX ไร้รอยต่อ

## 📋 Master Blueprint v2.1: The Core Product & Platform Split

### Repository Structure
- **`chonost-app`** (The Core Product): Tauri + Sidecar Desktop Application
- **`chonost-mcp`** (The Extensible Platform): Extensible Tool Platform

## 🔑 Core Concepts & Terminology (คำศัพท์เฉพาะของโปรเจกต์)

### The All-Seeing Eye
- ระบบ Project Manifest ที่ใช้ `watchdog` ในการสแกนและสร้างดัชนีไฟล์ทั้งหมด
- ใช้สำหรับการติดตามการเปลี่ยนแปลงไฟล์และสร้าง metadata

### The Forge
- ระบบ Code Interpreter ที่ใช้ Jupyter Kernel ผ่าน Docker
- สำหรับการรันโค้ดแบบ interactive และการทดลอง

### The Trinity Layout
- UI หลัก 3 ส่วน:
  - **ซ้าย**: Explorer (File/Project Browser)
  - **กลาง**: Editor/Whiteboard (Code Editor + Excalidraw)
  - **ขวา**: Chat/Tools (AI Chat + Tool Panel)

### The Living Dictionary
- ระบบ RAG (Retrieval-Augmented Generation) สำหรับสร้างสารานุกรมส่วนตัว
- ใช้สำหรับการคุยกับเอกสารและความรู้

### Lego Model
- ปรัชญาหลักในการสร้างทุกอย่างให้เป็นโมดูลที่เชื่อมต่อกันได้
- ทุก component ต้องเป็น reusable และ pluggable

### MCP (Master Control Program)
- ศูนย์กลางการเชื่อมต่อ Tool และบริการภายนอก
- เป็น "ท่าเรือ" ของระบบที่จัดการการสื่อสารระหว่าง components

### AI Agents
- "แขนขา" ของระบบที่ทำงานเฉพาะทางภายใต้การควบคุมของ AI Trinity (สมอง)
- แต่ละ agent มีหน้าที่เฉพาะและสามารถทำงานแบบ autonomous

## 🛠️ Tech Stack & Standards

### chonost-app (The Core Product)
```typescript
// Frontend Stack
- React + Vite
- TypeScript (strict mode)
- TailwindCSS
- @excalidraw/excalidraw (สำหรับ Whiteboard)

// Backend Stack (Sidecar)
- FastAPI
- Pydantic V2
- SQLAlchemy 2.0 (Async)
- LiteLLM (AI Model Management)
- Qdrant (Vector Database)
- Sentence-Transformers (Embeddings)
- Dramatiq + Redis (Task Queue)
- Jupyter Client (Code Execution)
```

### chonost-mcp (The Extensible Platform)
```python
# Server Stack
- FastAPI
- WebSockets
- HTTPX (HTTP Client)

# Database
- SQLite (จัดการด้วย Alembic)
```

## ⚡ Golden Rules (กฎการทำงานร่วมกัน)

### 1. ยึดตามพิมพ์เขียวและ Tech Stack
- ใช้เฉพาะ technologies ที่กำหนดใน Tech Stack
- ไม่เพิ่ม dependencies ที่ไม่จำเป็น
- ตรวจสอบ compatibility ก่อนเพิ่ม package ใหม่

### 2. ให้ข้อมูลครบถ้วนเสมอ
- **โดยปริยาย ให้ตอบแบบละเอียดและครบถ้วนเสมอ**
- **ห้ามสรุปหรือย่อความ** เว้นแต่ผู้ใช้จะสั่งอย่างชัดเจน
- เป้าหมายคือความสมบูรณ์ของข้อมูล ไม่ใช่ความกระชับ

### 3. ตรวจสอบ Syntax ก่อนส่ง
- **ก่อนจะสร้างโค้ดที่มี Syntax เฉพาะ** (เช่น Mermaid, YAML, JSON) ให้ตรวจสอบความถูกต้องเสมอ
- หากไม่แน่ใจให้ระบุว่า "ไม่แน่ใจเรื่อง Syntax"

### 4. อ้างอิง Core Concepts เสมอ
- **เวลาทำงานที่เกี่ยวข้องกับฟีเจอร์หลัก** ให้อ้างอิงชื่อจากลิสต์ Core Concepts เสมอ
- ตัวอย่าง: "กำลังสร้าง Logic การ Indexing สำหรับ The All-Seeing Eye"
- เพื่อเป็นการยืนยัน Context และความเข้าใจ

### 5. Coding Style Standards
```python
# Python
- Black (code formatting)
- Ruff (linting)
- Type hints ครบถ้วน
- Docstrings สำหรับทุก function
```

```typescript
// TypeScript
- Prettier (code formatting)
- ESLint (linting)
- Strict mode
- Interface definitions ครบถ้วน
- JSDoc สำหรับทุก function
```

### 6. Architecture Principles
- **Modular Design**: ทุก component ต้องเป็น independent และ reusable
- **Type Safety**: ใช้ TypeScript และ Pydantic อย่างเคร่งครัด
- **Async First**: ใช้ async/await สำหรับ I/O operations
- **Error Handling**: จัดการ error อย่างเหมาะสมและให้ informative messages

### 7. File Structure Guidelines
```
chonost-app/
├── src/
│   ├── components/     # React components
│   ├── services/       # API services
│   ├── stores/         # State management
│   ├── types/          # TypeScript types
│   └── utils/          # Utility functions
├── src-tauri/          # Tauri backend
└── sidecar/            # FastAPI sidecar

chonost-mcp/
├── src/
│   ├── agents/         # AI Agents
│   ├── core/           # Core business logic
│   ├── mcp/            # MCP server
│   ├── services/       # External services
│   └── utils/          # Utility functions
└── tests/              # Test files
```

### 8. Database Guidelines
- ใช้ Alembic สำหรับ migrations
- กำหนด models ด้วย SQLAlchemy 2.0 syntax
- ใช้ async operations เสมอ
- ตั้งชื่อ table และ column ให้ชัดเจน

### 9. API Design Guidelines
- ใช้ FastAPI decorators และ Pydantic models
- กำหนด response models ครบถ้วน
- ใช้ proper HTTP status codes
- จัดการ CORS อย่างเหมาะสม

### 10. Testing Requirements
- Unit tests สำหรับ business logic
- Integration tests สำหรับ API endpoints
- Mock external dependencies
- Test error scenarios

## 🎨 UI/UX Guidelines

### The Trinity Layout Implementation
```typescript
// Layout Structure
interface TrinityLayout {
  leftPanel: ExplorerPanel;    // File/Project browser
  centerPanel: EditorPanel;    // Code editor + Whiteboard
  rightPanel: ToolsPanel;      // Chat + Tools
}

// Responsive Design
- Mobile-first approach
- Collapsible panels
- Keyboard shortcuts
- Dark/Light theme support
```

### Excalidraw Integration
- ใช้ @excalidraw/excalidraw สำหรับ whiteboard functionality
- Integrate กับ code editor
- Support for diagrams และ flowcharts
- Export/Import capabilities

## 🔧 Development Workflow

### 1. Feature Development
1. อ้างอิง Core Concepts ที่เกี่ยวข้อง
2. ตรวจสอบ Tech Stack compatibility
3. วางแผน architecture
4. Implement ตาม standards
5. Test และ validate
6. Update documentation

### 2. Code Review Checklist
- [ ] Type safety ครบถ้วน
- [ ] Error handling เหมาะสม
- [ ] Performance considerations
- [ ] Security best practices
- [ ] Documentation updated
- [ ] Tests written

### 3. Deployment Guidelines
- ใช้ Docker สำหรับ containerization
- Environment variables สำหรับ configuration
- Health checks สำหรับ services
- Monitoring และ logging

## 🚨 Critical Reminders

### เมื่อทำงานกับ Core Features
- **The All-Seeing Eye**: ตรวจสอบ file watching และ indexing performance
- **The Forge**: ตรวจสอบ Docker security และ resource limits
- **The Trinity Layout**: ตรวจสอบ responsive design และ accessibility
- **The Living Dictionary**: ตรวจสอบ vector search accuracy และ relevance
- **MCP**: ตรวจสอบ tool integration และ error handling
- **AI Agents**: ตรวจสอบ autonomy และ safety measures

### Security Considerations
- ตรวจสอบ input validation
- ใช้ proper authentication/authorization
- ตรวจสอบ CORS settings
- ใช้ HTTPS ใน production
- ตรวจสอบ dependency vulnerabilities

### Performance Guidelines
- ใช้ async operations
- Implement caching strategies
- Monitor memory usage
- Optimize database queries
- Use proper indexing

## 📚 Documentation Standards

### Code Documentation
- JSDoc สำหรับ TypeScript
- Docstrings สำหรับ Python
- README files สำหรับทุก module
- API documentation ด้วย FastAPI auto-docs

### Architecture Documentation
- System architecture diagrams
- Component interaction flows
- Database schema documentation
- Deployment guides

---

**หมายเหตุ**: กฎเหล่านี้เป็นแนวทางหลักสำหรับการพัฒนาโปรเจกต์ Chonost Ecosystem ต้องปฏิบัติตามอย่างเคร่งครัดเพื่อรักษาคุณภาพและความสอดคล้องของโค้ด
